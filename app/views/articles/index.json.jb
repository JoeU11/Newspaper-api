@articles.map do |article|
  {
    id: article.id,
    title: article.title,
    author: article.author_name,
    photos: article.photos,
    text: article.text,
    created_at: article.created_at
  }
end
